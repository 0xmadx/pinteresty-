# Project Rules

## Architecture & Workflow Rules

**Rule 1: Testing and Discovery First, Pipeline Second**
Always build the Endpoints first to see the big picture. Test and verify all endpoints live to discover what actually works. Keep ONLY the verified endpoints, and THEN build Pipelines on top of them. Do not build heavy architecture on weak or untested endpoints.
